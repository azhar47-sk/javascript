Example 1: Using toUpperCase()
<script>
// js program to perform string comparison

const string1 = 'JavaScript Program';
const string2 = 'javascript program';

// compare both strings
const result1 = string1.toUpperCase() === string2.toUpperCase();
if(result1) {
document.write('<b>Result1:</b>The strings are similar.');
} else {
document.write('<b>Result1:</b>The strings are not similar.');
}
</script>


Example 2: JS String Comparison Using RegEx
<script>// program to perform string comparison

const string3 = 'JavaScript Program';
const string4 = 'javascript program';

// create regex
const pattern = new RegExp(string3, "gi");

// compare the stings
const result2 = pattern.test(string3)

if(result2) {
document.write('<b>Result2:</b>The strings are similar.');
} else {
document.write('<b>Result2:</b>The strings are not similar.');
}
</script>

Example 3: Using localeCompare()
<script>// program to perform case insensitive string comparison

const string5 = 'JavaScript Program';
const string6 = 'javascript program';

const result3 = string5.localeCompare(string2, undefined, { sensitivity: 'base' });

if(result3 == 0) {
document.write('<b>Result3:</b>The strings are similar.');
} else {
document.write('<b>Result3:</b>The strings are not similar.');
}
</script>
